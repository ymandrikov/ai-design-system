# Resume automatic migration without reopening setup

**Skill:** ai-design
**Route:** migrate
**Design:** fixtures/mini-ds/DESIGN.md
**Preparation:**

- Use a connected isolated mini-ds copy. Keep root DESIGN.md and all three required
  indexes. Add two documented project-owned wrappers whose contract work is unfinished.
- Seed `design-system/adoption.md` with an interrupted automatic mode-3 migration,
  batch size one, verification enabled, admission after ordinary audits and structural
  checks, and independent discovery after the whole selected scope. Include no
  connection history. Mark the first
  wrapper complete and the second pending; link the file from DESIGN.md.

**Request:**

> Resume the saved automatic migration and finish the remaining scope. Keep the
> existing mode, batch size and verification choice.

**Expected:**

- Enter standalone migrate directly and use saved decisions without reconstructing
  connection history or reading setup.
- Reconcile saved progress with current sources, retain the completed wrapper's valid
  evidence, finish the pending wrapper and continue across batches without a new
  continuation, mode, batch-size or verification question.
- Apply saved whole-set timing: admit complete existing contracts before one final
  independent discovery gate over the full selected set. Do not substitute per-batch
  gates. Preserve these conditions through completion.
- If the final gate fails or is unavailable, retain admitted entities and completed
  connection, save migration as incomplete and return its blocker. Do not reopen
  setup or offer setup's A/B/C/D question.
