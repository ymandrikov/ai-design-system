# Connect without mandatory page adoption

**Skill:** design-system
**Route:** setup
**Design:** fixtures/mini-ds/DESIGN.md
**Request:**

> Connect mini-ds to the framework using its existing sources. You may inspect
> pages you find automatically. No adoption or independent demonstration is
> requested; no page has been selected. Complete the ordinary connection.
> Run the applicable verification checks.

**Expected:**

- Use plain language matching the user and explain contracts at first mention.
- Preserve existing agent instructions and add the main DESIGN.md/design-system
  pointer to AGENTS.md. Preserve CLAUDE.md and existing symlinks; create no symlinks.
- Move existing component/layout/pattern contracts to their standard directories,
  preserving content and statuses. Generate uppercase indexes and repair affected
  references without changing code or tokens. This is connection work, not adoption.
- Inspect useful pages without requiring a selected page or a demonstration.
  Record missing sources/tools honestly. Read applicable project rules without
  auditing unrelated repository skills or reorganising instructions.
- Preserve existing journal/archive records and links. Empty new journals are optional.
- After preparation, show remaining undocumented reusable UI and offer full automatic
  contracts-only migration or connection only. Reuse the requested verification;
  no verification question, procedure menu, mode choice or batch-size question.
- Reconcile changed connection artifacts and report the connection independently.
  Start no migration without explicit confirmation; lack of a migration answer does
  not invalidate connection completion. Report remaining uncovered UI.

**Independent variants:**

- **Verification unspecified:** Remove the verification sentence. After preparation,
  ask about migration and run-or-skip checks in the same round, recommending run.
  Wait before checks. Declining migration still permits connection verification.
- **Migration accepted:** Accept full migration. Pass mode 3, whole-project scope and
  saved verification to migrate; continue all batches without more startup questions.
- **Migration declined:** Decline migration and run connection checks. Finish the
  connection and report uncovered UI without authoring missing contracts.
