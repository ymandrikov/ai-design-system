# Connect and automatically document the requested scope

**Skill:** design-system
**Route:** setup
**Design:** fixtures/mini-ds/DESIGN.md
**Preparation:**

- In the isolated copy, add 12 project-owned React wrappers around Button under
  `react/actions/`: Save, Cancel, Retry, Refresh, Download, Upload, Print, Share,
  Copy, Edit, Archive and Delete action buttons. Each accepts only an onClick
  callback and renders its fixed text through Button; Delete uses danger, the
  others secondary. Export them from that directory's public entrypoint and add
  a product usage file exercising all 12. Give them no contracts or index entries.
- Link an authoritative source in DESIGN.md defining those labels, variants and
  immediate-action purposes. Keep existing contracts, statuses and tests intact.
- Include a private rendering helper inside one wrapper and an unwrapped React
  import in the usage file to exercise the scope boundary.

**Request:**

> Set up this project and add contracts for all our reusable components and layouts.

**Expected:**

- Prepare connection and treat the explicit request to document all reusable
  components and layouts as existing migration authority. Do not ask again, offer
  API analysis or select gradual adoption with mandatory pauses.
- Show the complete requested scope and remaining count; preserve existing statuses
  during connection and avoid redundant work on complete contracts.
- After preparation, explain applicable connection/migration checks and offer run or
  skip, recommending run. Wait for this missing choice before checks.
- Pass mode 3, the requested component/layout scope and verification to migrate.
  Save and link its work list, including the 12 wrappers, unmanaged Toggle and Stack.
  Group bindings and exclude product pages, private helpers and third-party dependencies.
- Order Button before its wrappers. Choose batch size internally, save progress and
  continue all batches without confirmation. Preserve runtime code and appearance.
- Ask about unresolved rules while continuing independent work; run independent
  discovery only when explicitly requested or required by project policy.
