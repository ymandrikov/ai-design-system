# Separate controls, layout and semantic surfaces

**Skill:** ai-design
**Route:** discovery
**Design:** fixtures/mini-ds/DESIGN.md
**Request:**

> On the settings page (React), arrange a warning banner and a Save button
> vertically with equal spacing. The inline warning says the API token has
> expired; Save submits the form.

**Expected:**

- Account for all three needs: Stack for vertical layout, no managed warning component, and Button for Save. A missing warning component must not erase the valid layout/action choices. Accept one paragraph, a table or separate decisions; block count is irrelevant.

- Discovery leaves project files unchanged.
