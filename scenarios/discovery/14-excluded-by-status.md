# The only fitting entry is hidden

**Skill:** ai-design
**Route:** discovery
**Design:** fixtures/tie-ds/DESIGN.md
**Request:**

> In the pipeline filter bar (React), add a field where the user picks one
> or more teams from about 30 teams, typing to find them.

**Expected:**

- No discoverable component fits multiple selection. Combobox and Autocomplete are single-choice; MultiPick is hidden and cannot be selected or offered as fallback. References to an already explained rejection reason are sufficient.

- Discovery leaves project files unchanged.
