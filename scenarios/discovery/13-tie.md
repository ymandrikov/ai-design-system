# Two valid choices differ only by preference

**Skill:** design-system
**Route:** discovery
**Design:** fixtures/tie-ds/DESIGN.md
**Request:**

> In the billing address form (React), add a country field. There are about
> 40 countries, all available at render time. Users type to choose exactly
> one. Either an inline list or a popup is acceptable; choose one.

**Expected:**

- Combobox and Autocomplete both fit. Accept either choice without requiring the other to appear in the report. The request permits either presentation, so do not block progress on that preference.

- Discovery leaves project files unchanged.
