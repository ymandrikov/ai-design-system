# The task wins over the means, then the API decides

**Skill:** design-system
**Route:** discovery
**Design:** fixtures/mini-ds/DESIGN.md
**Request:**

> On the API keys page (React), next to each key, add an icon-only control
> with no visible text that copies the key to the clipboard. The key is
> shown on the page.

**Expected:**

- No managed component expresses the requested icon-only copy control: CopyButton requires visible text. Any stated rejection reason must reflect the actual API.

- Discovery leaves project files unchanged.
