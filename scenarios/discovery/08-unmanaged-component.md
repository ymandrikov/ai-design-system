# Ignore an unmanaged implementation

**Skill:** ai-design
**Route:** discovery
**Design:** fixtures/mini-ds/DESIGN.md
**Request:**

> On the notification settings page (React), add a control that turns email
> notifications on or off. The change saves immediately when the user flips
> it.

**Expected:**

- No managed component fits the on/off setting. The existing unmanaged Toggle may be mentioned, but must not be presented as an eligible managed choice.

- Discovery leaves project files unchanged.
