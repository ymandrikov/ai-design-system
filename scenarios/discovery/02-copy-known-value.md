# Copy a known value

**Skill:** design-system
**Route:** discovery
**Design:** fixtures/mini-ds/DESIGN.md
**Request:**

> On the pipeline settings page (React), add a control next to the displayed
> webhook URL that lets the user copy that URL. The URL is known when the
> page renders.

**Expected:**

- Choose CopyButton to copy the known URL. Its public API covers the task.

- Discovery leaves project files unchanged.
