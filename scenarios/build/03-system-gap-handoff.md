# Infer use and hand a system gap to craft

**Skill:** ai-design
**Design:** fixtures/mini-ds/DESIGN.md
**Request:**

> Build a React expired-token warning for a settings screen at
> fixtures/mini-ds/product/expired-token-warning.tsx. It displays only the static
> text "Your API token has expired." An ordinary paragraph using inherited page
> styles is acceptable for this page. Several settings areas will eventually need
> the same capability. Record the reusable need and tell the design-system team
> what is missing. Browser access is unavailable in this run.

**Expected:**

- Infer use from the product request and inspect managed coverage before fallback.
- Deliver the authorised local warning; check its actual rendered text with a
  focused project check and report browser evidence as unavailable.
- Record one reusable warning gap and describe the craft handoff in the response.
  Do not send an external message to the team.
- Preserve shared component implementations, contracts, indexes, rules and tokens.
  The gap and future reuse do not authorise building or admitting a shared component.
