# Prioritise gaps and prepare a decision

**Skill:** ai-design
**Design:** fixtures/mini-ds/DESIGN.md
**Preparation:** In the isolated copy, replace the gap journal with these entries.
Their Source and Need are supplied request facts, not proof of a design rule.

| Title | Source / Need | Actual result | Expected result | Evidence |
| --- | --- | --- | --- | --- |
| Multiple-value selection | Current access-settings task: choosing several team members is blocked | Existing pickers only select one value | A discoverable picker for multiple values | NativeSelect and Select contracts; estimated repair is larger than a documentation edit |
| Compact density policy | Several consumers need to decide whether otherwise comparable forms may use different densities | No authoritative conditions for compact versus regular density have been identified | An explicitly accepted standard or explicit freedom with scope and selection conditions | DESIGN.md and linked rules; current visual variants do not establish permission |
| Missing grouping recipe | A future settings area may need a reusable grouping recipe | No concrete task or composition constraints were recorded | A suitable reusable recipe can be selected | Impact, reach and request conditions are unknown; described as a quick documentation task |

**Request:**

> Разбери открытые гэпы, приведи журнал в порядок и предложи, в каком порядке
> ими заниматься. На вопросы о новых правилах сейчас никто не ответит.

**Expected:**

- Route the natural-language request to triage and update only the gap journal.
- Verify selection limits and prioritise the current blocked multiple-selection
  task over speculative work; explain consequences, dependencies and known reach.
  A cheap documentation change alone is not a reason to put it first. Use no
  numerical scoring and do not invent missing impact or effort estimates.
- Keep the density-policy gap open. Prepare a concrete question, alternatives and a recommendation; current values
  or examples cannot establish a new normative rule. Continue the other entries.
- Investigate the existing settings pattern for Missing grouping recipe; retain
  the uncertain entry with the missing task facts rather than asserting coverage,
  deleting it for lack of evidence or starting a repository-wide audit.
- Distinguish work ready for craft from work awaiting facts or a decision.
  Do not implement a multi-select, author density rules or create a new pattern.
