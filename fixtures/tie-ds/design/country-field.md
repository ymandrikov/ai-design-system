# Billing country field

The input has the visible label Country. Typing filters the fixed country
list; selecting one item supplies the form value.

Prefer showing matches in a popup layered over the next field. An inline
list that pushes the next field down is acceptable too. Both layouts fit
this screen; the popup is the design preference.
