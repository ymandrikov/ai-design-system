# <Design system> <components | layouts>

Use this template for COMPONENTS.md or LAYOUTS.md in design-system/.
Each component/layout has one entry in its group's index. Labels are display names;
read authoritative id/status from the linked contract's frontmatter. Keep selection
rules in contracts. Links resolve from this index. Empty indexes are valid.

- **<Display name>**
  - Purpose: <Short task summary derived from the contract, without selection rules.>
  - Contract: [<Entity name>](<components-or-layouts>/<filename>.md)

Patterns live in PATTERNS.md with their full descriptions and contents links.
Entities absent from these indexes and pattern sections are unmanaged.
