# <Design system> <components | layouts | patterns>

Use this template for COMPONENTS.md, LAYOUTS.md or PATTERNS.md in design-system/.
Each component/layout/pattern has one entry in its group's index. Labels are display names;
read authoritative id/status from the linked contract's frontmatter. Keep selection
rules in contracts. Links resolve from this index. Empty indexes are valid.

- **<Display name>**
  - Purpose: <Describe the supported tasks, means and material distinctions from
      related entities, using request language and facts from the contract.
      Use enough detail to avoid missing suitable candidates; no fixed length.
      Keep exact applicability conditions in the contract.>
  - Contract: [<Entity name>](<components-or-layouts-or-patterns>/<filename>.md)

Entities absent from these indexes are unmanaged.
